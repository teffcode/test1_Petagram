<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coursera.tefa.appmascotas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_Secundaria" />
        <activity android:name=".ActivityMenuContacto" />
        <activity
            android:name=".ScrollingActivityMenuAcercaDe"
            android:label="@string/title_activity_scrolling_menu_acerca_de"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>